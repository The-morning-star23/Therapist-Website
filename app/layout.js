import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Dr. Serena Blake - Licensed Therapist & Mental Health Counselor",
  description: "Professional therapy services with Dr. Serena Blake. Specializing in anxiety, depression, trauma, and relationship counseling. Book your consultation today.",
  keywords: "therapist, mental health, counseling, therapy, anxiety, depression, trauma, relationship counseling",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
